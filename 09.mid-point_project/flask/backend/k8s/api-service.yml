apiVersion: v1
kind: Service
metadata:
  name: dukaan-app-service
  namespace: dukaan
spec:
  selector:
    app: dukaan-app
  ports:
    - name: http
      port: 80
      targetPort: 5000
      nodePort: 30080
  type: NodePort