apiVersion: apps/v1
kind: Deployment
metadata:
  name: spring-boot-demo
spec:
  replicas: 3
  selector:
    matchLabels:
      app: spring-boot-demo
  strategy: {}
  template:
    metadata:
      labels:
        app: spring-boot-demo
    spec:
      containers:
        - name: spring-boot-demo
          image: spring-hello-world-kube
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          env:
            - name: ENV_VALUE
              valueFrom:
                configMapKeyRef:
                  name: spring-boot-demo-config
                  key: ENV_VALUE
          resources:
            limits:
              memory: "1Gi"
              cpu: "500m"
            requests:
              memory: "512Mi"
              cpu: "250m"
# apiVersion: apps/v1
# kind: Deployment
# metadata:
#   name: my-api
#   namespace: my-api
#   labels:
#     app: my-api
# spec:
#   replicas: 3
#   selector:
#     matchLabels:
#       app: my-api
#   strategy:
#     type: RollingUpdate
#     rollingUpdate:
#       maxSurge: 1
#       maxUnavailable: 1
#   template:
#     metadata:
#       labels:
#         app: my-api
#     spec:
#       terminationGracePeriodSeconds: 30
#       containers:
#         - name: my-api
#           image: your-dockerhub/my-api:1.0.0
#           imagePullPolicy: Always
#           ports:
#             - containerPort: 3000
#           envFrom:
#             - configMapRef:
#                 name: my-api-config
#             - secretRef:
#                 name: my-api-secret
#           resources:
#             requests:
#               cpu: "100m"
#               memory: "128Mi"
#             limits:
#               cpu: "500m"
#               memory: "256Mi"
